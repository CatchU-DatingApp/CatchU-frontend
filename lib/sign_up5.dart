import 'package:flutter/material.dart';

class SignUpPage5 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Next Step')),
      body: Center(child: Text('Next signup step')),
    );
  }
}
